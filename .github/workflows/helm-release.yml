name: Helm Chart Release

permissions:
  contents: write
  packages: write

on:
  workflow_dispatch:
    inputs:
      version_bump:
        description: 'Version bump type for Helm chart release'
        required: true
        default: 'patch'
        type: choice
        options:
          - major
          - minor
          - patch

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  helm:
    uses: fairagro/m4.2_middleware_devinfra/.github/workflows/reusable-helm-release.yml@main
    with:
      version_bump: ${{ inputs.version_bump }}
      chart_dir: helm/harvester
      chart_name: fairagro-advanced-middleware-harvester-chart
      dockerhub_namespace: zalf
      require_main: true
      helm_install_name: fairagro-harvester
    secrets: inherit
