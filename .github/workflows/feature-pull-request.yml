name: Feature Pull Request

permissions:
  contents: read
  pull-requests: read
  security-events: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - 'main'

concurrency:
  group: feature-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  detect-changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      code: ${{ steps.filter.outputs.code }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            code:
              - 'middleware/**'
              - 'pyproject.toml'
              - 'uv.lock'
              - 'docker/**'
              - 'scripts/**'
              - '.github/workflows/**'
              - 'docker-bake.hcl'
              - 'versions.env'
              - 'ruff.toml'
              - 'mypy.ini'
              - '.pylintrc'
              - '.pre-commit-config.yaml'
              - '.bandit'

  code-quality:
    name: Code Quality
    needs: detect-changes
    uses: fairagro/m4.2_middleware_devinfra/.github/workflows/reusable-code-quality.yml@e2c2ab4cbeab6c33c36fad6fbad0cb85958efb07
    with:
      python_package_root: middleware
      components: '["harvester"]'
      mypy_path: stubs:middleware/inspire/src:middleware/harvester/src:middleware/linked_data/src:middleware/generic/src:middleware/payload/src:middleware/linked_data/tests/unit:middleware/inspire/tests/unit:middleware/payload/tests/unit
      pylint_source_roots: middleware/linked_data/tests/unit,middleware/inspire/tests/unit,middleware/payload/tests/unit
      skip: ${{ needs.detect-changes.outputs.code != 'true' }}
    secrets: inherit

  build:
    name: Build
    needs: detect-changes
    if: needs.detect-changes.outputs.code == 'true'
    uses: fairagro/m4.2_middleware_devinfra/.github/workflows/reusable-build.yml@main
    with:
      components: '["harvester"]'
      image_base_name: fairagro-advanced-middleware
    secrets: inherit

  check:
    name: Check
    needs: [detect-changes, build]
    # Docs-only: build is skipped → still run check with skip=true (required checks).
    # Code PR: only run check when build succeeded (avoid empty-version secondary failure).
    if: >-
      always()
      && needs.detect-changes.result == 'success'
      && (needs.build.result == 'success' || needs.build.result == 'skipped')
    uses: fairagro/m4.2_middleware_devinfra/.github/workflows/reusable-check.yml@main
    with:
      # toJSON/fromJSON avoids Actions LS false positive on needs.build.outputs.version
      # when the build job is conditional (docs-only skip path).
      version: ${{ fromJSON(toJSON(needs.build)).outputs.version || '' }}
      components: '["harvester"]'
      image_base_name: fairagro-advanced-middleware
      skip: ${{ needs.detect-changes.outputs.code != 'true' }}
    secrets: inherit
