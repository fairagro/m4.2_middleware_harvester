# Demo Environment

The local development and demo environment is self-contained.

## Requirements

- [ ] Compose stack standing up the required demo instances (local Postgres database or local web instances if required).
- [ ] Provide a `start.sh` to initialize the workflow via `docker` or local CLI.
- [ ] Provide a basic configurable test `config.yaml` for testing harvesting features.
